name: contentflow_creator
description: "ContentFlow Creator App for Amin Cap"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.12.0
  cloud_firestore: ^5.6.5
  firebase_storage: ^12.4.4
  file_picker: ^8.1.7
  firebase_auth: ^5.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true